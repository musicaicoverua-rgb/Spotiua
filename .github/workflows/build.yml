name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.4'

    - name: Inject Gradle Hook, Dependencies and Fix Settings
      run: |
        unzip "Kimi_Agent_OpenWave Native Music App.zip"
        
        sed -i 's/FAIL_ON_PROJECT_REPOS/PREFER_SETTINGS/g' openwave/android/settings.gradle.kts
        sed -i '/resourceConfigurations/d' openwave/android/app/build.gradle.kts
        
        cat << 'EOF' >> openwave/android/app/build.gradle.kts
        
        // Auto-inject missing library dependencies that AI forgot
        dependencies {
            implementation("androidx.media3:media3-exoplayer:1.3.1")
            implementation("androidx.media3:media3-session:1.3.1")
            implementation("androidx.media3:media3-common:1.3.1")
            implementation("androidx.media3:media3-ui:1.3.1")
            implementation("androidx.media:media:1.7.0")
            implementation("com.jakewharton.timber:timber:5.0.1")
        }
        
        tasks.matching { it.name.startsWith("merge") && it.name.endsWith("Resources") }.configureEach {
            doFirst {
                val uStr = 92.toChar() + "u{str}"
                val regArtist = Regex("<string name=\"artist_name\".*?</string>", RegexOption.DOT_MATCHES_ALL)
                val regUser = Regex("<string name=\"username_optional\".*?</string>", RegexOption.DOT_MATCHES_ALL)
                
                fun sanitizeDir(dir: File) {
                    if (dir.exists()) {
                        dir.walkTopDown().filter { it.name.endsWith(".xml") }.forEach { file ->
                            try {
                                var text = file.readText()
                                var changed = false
                                if (text.contains("artist_name")) { text = text.replace(regArtist, ""); changed = true }
                                if (text.contains("username_optional")) { text = text.replace(regUser, ""); changed = true }
                                if (text.contains(uStr)) { text = text.replace(uStr, "str"); changed = true }
                                if (changed) { file.writeText(text) }
                            } catch(e: Exception) {}
                        }
                    }
                }
                sanitizeDir(File(System.getProperty("user.home"), ".gradle/caches/transforms-3"))
                sanitizeDir(File(projectDir, "src/main/res"))
            }
        }
        EOF

    - name: Generator of Missing LLM Resources
      run: |
        RES_DIR="openwave/android/app/src/main/res"
        mkdir -p "$RES_DIR/xml" "$RES_DIR/values" "$RES_DIR/mipmap-anydpi-v26" "$RES_DIR/drawable"
        
        echo '<?xml version="1.0" encoding="utf-8"?><data-extraction-rules><cloud-backup><exclude domain="sharedpref" path="."/></cloud-backup></data-extraction-rules>' > "$RES_DIR/xml/data_extraction_rules.xml"
        echo '<?xml version="1.0" encoding="utf-8"?><full-backup-content><exclude domain="sharedpref" path="."/></full-backup-content>' > "$RES_DIR/xml/backup_rules.xml"
        
        # Strings + Missing Media Notification Texts
        echo '<?xml version="1.0" encoding="utf-8"?><resources><string name="app_name">OpenWave</string><string name="playback_channel_name">Playback</string><string name="playback_channel_description">Media Controls</string></resources>' > "$RES_DIR/values/kimi_missing_strings.xml"
        echo '<?xml version="1.0" encoding="utf-8"?><resources><style name="Theme.OpenWave" parent="android:Theme.DeviceDefault.NoActionBar"/></resources>' > "$RES_DIR/values/kimi_missing_themes.xml"
        
        # Generate missing Media Notification Icons (Placeholders)
        VECTOR_XML='<?xml version="1.0" encoding="utf-8"?><vector xmlns:android="http://schemas.android.com/apk/res/android" android:width="24dp" android:height="24dp" android:viewportWidth="24" android:viewportHeight="24"><path android:fillColor="#FFFFFF" android:pathData="M8,5v14l11,-7z"/></vector>'
        echo "$VECTOR_XML" > "$RES_DIR/drawable/play.xml"
        echo "$VECTOR_XML" > "$RES_DIR/drawable/pause.xml"
        echo "$VECTOR_XML" > "$RES_DIR/drawable/previous.xml"
        echo "$VECTOR_XML" > "$RES_DIR/drawable/next.xml"
        
        cat << 'EOF' > "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml"
        <?xml version="1.0" encoding="utf-8"?>
        <vector xmlns:android="http://schemas.android.com/apk/res/android" android:width="108dp" android:height="108dp" android:viewportWidth="108" android:viewportHeight="108">
            <path android:fillColor="#1DB954" android:pathData="M0,0h108v108h-108z"/>
        </vector>
        EOF
        cp "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml" "$RES_DIR/mipmap-anydpi-v26/ic_launcher_round.xml"

    - name: The Kotlin Auto-Reorganizer
      run: |
        echo "Deep structural reorganization of Kotlin files..."
        python3 - <<'EOF'
        import os, re

        valid_starts = (
            '@', '//', '/*',
            'class ', 'interface ', 'object ', 'fun ', 'val ', 'var ', 'typealias ',
            'public ', 'private ', 'protected ', 'internal ', 'expect ', 'actual ',
            'final ', 'open ', 'abstract ', 'sealed ', 'const ', 'external ',
            'override ', 'lateinit ', 'tailrec ', 'vararg ', 'suspend ', 'inner ',
            'enum ', 'annotation ', 'companion ', 'inline ', 'noinline ',
            'crossinline ', 'reified ', 'infix ', 'operator ', 'data '
        )

        for root, dirs, files in os.walk('openwave/android/app/src/main/java'):
            for f in files:
                if not (f.endswith('.kt') or f.endswith('.java')): continue
                path = os.path.join(root, f)

                try:
                    with open(path, 'r', encoding='utf-8') as file:
                        content = file.read()

                    b3 = chr(96) * 3
                    pattern = b3 + r'(?:kotlin|java|kt)?\n?(.*?)' + b3
                    blocks = re.findall(pattern, content, re.DOTALL | re.IGNORECASE)
                    if blocks: content = max(blocks, key=len)
                    
                    content = content.replace(b3, '')
                    content = re.sub(r'^(?:kotlin|java|kt)\b', '', content, flags=re.IGNORECASE).strip()

                    lines = content.split('\n')
                    package_line = ""
                    imports = set()
                    body_lines = []

                    for line in lines:
                        stripped = line.strip()
                        if stripped.startswith('package '):
                            if not package_line: package_line = stripped
                        elif stripped.startswith('import '):
                            imports.add(stripped)
                        else:
                            body_lines.append(line)

                    # Auto-inject Timber import if missing
                    body_text = '\n'.join(body_lines)
                    if 'Timber.' in body_text and 'import timber.log.Timber' not in '\n'.join(imports):
                        imports.add('import timber.log.Timber')

                    start_idx = 0
                    for i, line in enumerate(body_lines):
                        stripped = line.strip()
                        if not stripped: continue
                        is_valid = False
                        for vs in valid_starts:
                            if stripped.startswith(vs):
                                is_valid = True
                                break
                        if is_valid:
                            start_idx = i
                            break

                    body_lines = body_lines[start_idx:]

                    if not package_line:
                        pkg_path = ""
                        if 'java/' in path: pkg_path = path.split('java/')[1]
                        elif 'kotlin/' in path: pkg_path = path.split('kotlin/')[1]
                        if pkg_path:
                            pkg_dir = os.path.dirname(pkg_path)
                            pkg_name = pkg_dir.replace('/', '.').replace('\\', '.')
                            if pkg_name: package_line = f"package {pkg_name}"

                    final_content = []
                    if package_line:
                        final_content.append(package_line)
                        final_content.append("")
                    for imp in sorted(list(imports)):
                        final_content.append(imp)
                    if imports: final_content.append("")
                    final_content.extend(body_lines)

                    with open(path, 'w', encoding='utf-8') as f_out:
                        f_out.write('\n'.join(final_content))
                except Exception as e:
                    pass
        EOF

    - name: The Ultimate Room SuperHealer
      run: |
        echo "Scanning project for missing Database entities and DAOs..."
        python3 - <<'EOF'
        import os, re

        src_dir = 'openwave/android/app/src/main/java'
        all_classes = set()
        for r, d, files in os.walk(src_dir):
            for f in files:
                if f.endswith('.kt') or f.endswith('.java'):
                    try:
                        with open(os.path.join(r, f), 'r', encoding='utf-8') as kf:
                            content = kf.read()
                            for match in re.finditer(r'(?:class|interface|object)\s+([a-zA-Z0-9_]+)', content):
                                all_classes.add(match.group(1))
                    except: pass

        db_path = None
        for r, d, files in os.walk(src_dir):
            if 'AppDatabase.kt' in files:
                db_path = os.path.join(r, 'AppDatabase.kt')
                break

        if db_path:
            with open(db_path, 'r', encoding='utf-8') as f:
                content = f.read()

            db_pkg = "com.openwave.app.data.local"
            pkg_match = re.search(r'^package\s+([a-zA-Z0-9_.]+)', content, re.MULTILINE)
            if pkg_match: db_pkg = pkg_match.group(1)

            imports = {}
            for imp in re.findall(r'^import\s+([a-zA-Z0-9_.]+)', content, re.MULTILINE):
                class_name = imp.split('.')[-1]
                imports[class_name] = imp

            missing_entities = []
            missing_converters = []
            missing_daos = []

            entities_match = re.search(r'@Database\s*\(\s*entities\s*=\s*[\[\{](.*?)[\]\}]', content, re.DOTALL)
            if entities_match:
                for e in entities_match.group(1).split(','):
                    c_clean = re.sub(r'::class(\.java)?|\.class', '', e).strip()
                    if '.' in c_clean: c_clean = c_clean.split('.')[-1]
                    if c_clean and c_clean not in all_classes: missing_entities.append(c_clean)

            converters_match = re.search(r'@TypeConverters\s*\(\s*(?:value\s*=\s*)?[\[\{]?(.*?)[\]\}]?\s*\)', content, re.DOTALL)
            if converters_match:
                for c in converters_match.group(1).split(','):
                    c_clean = re.sub(r'::class(\.java)?|\.class', '', c).strip()
                    if '.' in c_clean: c_clean = c_clean.split('.')[-1]
                    if c_clean and c_clean not in all_classes: missing_converters.append(c_clean)

            for dao_match in re.finditer(r'abstract\s+(?:fun|val)\s+[a-zA-Z0-9_]+\s*(?:\(\s*\))?\s*:\s*([a-zA-Z0-9_]+)', content):
                dao_name = dao_match.group(1)
                if dao_name not in all_classes: missing_daos.append(dao_name)

            def create_missing(class_name, c_type):
                expected_pkg = db_pkg
                if class_name in imports:
                    full_imp = imports[class_name]
                    expected_pkg = '.'.join(full_imp.split('.')[:-1])

                rel_path = expected_pkg.replace('.', '/')
                full_dir = os.path.join(src_dir, rel_path)
                os.makedirs(full_dir, exist_ok=True)
                file_path = os.path.join(full_dir, f"{class_name}.kt")

                code = f"package {expected_pkg}\n\n"
                if c_type == 'entity':
                    code += f"import androidx.room.Entity\nimport androidx.room.PrimaryKey\n\n@Entity\ndata class {class_name}(\n    @PrimaryKey val id: String = \"default_id\"\n)\n"
                elif c_type == 'converter':
                    code += f"import androidx.room.TypeConverter\n\nclass {class_name} {{\n    @TypeConverter\n    fun fromStr(value: String?): List<String> = value?.split(\",\") ?: emptyList()\n    @TypeConverter\n    fun toStr(list: List<String>?): String = list?.joinToString(\",\") ?: \"\"\n}}\n"
                elif c_type == 'dao':
                    code += f"import androidx.room.Dao\n\n@Dao\ninterface {class_name} {{\n}}\n"

                with open(file_path, 'w', encoding='utf-8') as f: f.write(code)

            for e in missing_entities: create_missing(e, 'entity')
            for c in missing_converters: create_missing(c, 'converter')
            for d in missing_daos: create_missing(d, 'dao')
        EOF

    - name: Build APK
      run: |
        cd openwave/android
        gradle assembleDebug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Spotiua-Native-Final
        path: '**/*.apk'
